steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args: ['app', 'deploy']

options:
  logging:
    # Specify the bucket where build logs will be stored
    logsBucket: 'python_project_03202024'
